#include "main.h"

/**
* _isupper - Returns 1 if c is uppercase and 0 if not.
* @c: int type variable.
* Return: 1 if uppercase and 0 if lowercase.
*/
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
	return (0);
}
