#include "main.h"

/**
* _isdigit - Returns 1 if c is a digit and 0 if not.
* @c: int type variable.
* Return: 1 if digit and 0 if otherwise
*/
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1);
	else
		return (0);
	return (0);
}
